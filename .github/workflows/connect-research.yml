name: ArborLine Research Queue

on:
  schedule:
    - cron: "37 * * * *"
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - ".github/workflows/connect-research.yml"

permissions:
  contents: read
  id-token: write

concurrency:
  group: arborline-research-queue
  cancel-in-progress: false

jobs:
  research:
    name: Research qualified prospects
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Request GitHub OIDC token and run research queue
        shell: bash
        run: |
          set -euo pipefail

          separator="&"
          if [[ "${ACTIONS_ID_TOKEN_REQUEST_URL}" != *"?"* ]]; then
            separator="?"
          fi

          oidc_response="$(curl --fail --silent --show-error \
            -H "Authorization: Bearer ${ACTIONS_ID_TOKEN_REQUEST_TOKEN}" \
            "${ACTIONS_ID_TOKEN_REQUEST_URL}${separator}audience=arborline-connect-research")"
          oidc_token="$(printf '%s' "${oidc_response}" | jq -r '.value')"

          if [[ -z "${oidc_token}" || "${oidc_token}" == "null" ]]; then
            echo "GitHub did not issue an OIDC token."
            exit 1
          fi

          url="https://www.arborlineconnect.com/api/cron/connect-research"
          if [[ "${GITHUB_EVENT_NAME}" == "push" ]]; then
            url="${url}?segment=landscaping"
          fi

          curl --fail-with-body --silent --show-error \
            --retry 2 --retry-all-errors --max-time 240 \
            -H "Authorization: Bearer ${oidc_token}" \
            -H "User-Agent: ArborLine-GitHub-Research/1.0" \
            "${url}"
          echo
